name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
    paths:
      - 'dokumen-prd-hris-terintegrasi-2/src/**'
      - 'dokumen-prd-hris-terintegrasi-2/package.json'
      - 'dokumen-prd-hris-terintegrasi-2/vite.config.ts'
      - '.github/workflows/deploy-pages.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dokumen-prd-hris-terintegrasi-2
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: 'dokumen-prd-hris-terintegrasi-2/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript Check
        run: npx tsc --noEmit

      - name: Build Vite App
        run: npx vite build

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
